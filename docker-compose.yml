version: "3.8"

services:
  app:
    image: ssre_appdb:latest
    container_name: ssre_appdb
    restart: always
    env_file: .env
    depends_on:
      - mongo
      - postgres
    ports:
      - "3002:3002"

  mongo:
    build: ./mongo
    container_name: mongo
    restart: always
    volumes:
      - mongo_data:/data/db

  postgres:
    build: ./postgres
    container_name: postgres
    restart: always
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  mongo_data:
  pg_data:
